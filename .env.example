# Environment Variables Template
# Copy this file to .env.local and fill in your values

# MongoDB Atlas Connection
# IMPORTANT: If you encounter DNS resolution issues on Windows, use the standard format instead of SRV
# 
# Option 1: SRV Format (recommended, but may have DNS issues on some Windows systems)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/waste-coin-db?retryWrites=true&w=majority&appName=yourapp
#
# Option 2: Standard Format (use if SRV format fails)
# Get your actual server addresses from MongoDB Atlas: Connect > Drivers > View Full Code Sample
# MONGODB_URI=mongodb://username:password@host1:27017,host2:27017,host3:27017/waste-coin-db?ssl=true&authSource=admin&retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/waste-coin-db?retryWrites=true&w=majority

# JWT Secret (Run: npm run generate-secrets to create secure random values)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Encryption Secret for Private Keys (Run: npm run generate-secrets to create secure random values)
ENCRYPTION_SECRET=your-32-byte-hex-encryption-key-change-this

# Blockchain Configuration
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
# Or use Alchemy: https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY

# Officer Wallet (for minting coins)
OFFICER_PRIVATE_KEY=your-officer-wallet-private-key-with-sepolia-eth

# Smart Contract Address (will be filled after deployment)
WASTE_COIN_CONTRACT_ADDRESS=

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000